# Confluent Schema Registry — dev configuration
# Registry URL: http://localhost:8081

# Subject naming: <topic>-value
# Recommended for production: AVRO schemas in messaging/schema-registry/schemas/

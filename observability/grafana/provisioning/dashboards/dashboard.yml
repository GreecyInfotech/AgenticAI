apiVersion: 1

providers:
  - name: EAAP
    folder: Enterprise Agentic AI
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards

dashboards:
  - name: EAAP Overview
    options:
      path: /etc/grafana/provisioning/dashboards/eaap-overview.json
